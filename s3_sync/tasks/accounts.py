from s3_sync.providers.s3 import sync_file
from s3_sync.providers.pg import pg_query

# query = "SELECT id, file_file_name, thumbnail_file_name, processing, processing FROM media_attachments ORDER BY id LIMIT 1000;"

# attachments = pg_query(query)
